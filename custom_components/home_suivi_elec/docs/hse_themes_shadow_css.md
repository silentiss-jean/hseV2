# Shadow theme styles — `hse_themes.shadow.css`

Target file:

- `custom_components/home_suivi_elec/web_static/shared/styles/hse_themes.shadow.css`

AI-first: what theme scopes it defines.
Human layer: troubleshooting.

---

## Purpose

Applies theme variables inside the panel shadow DOM.

Document:

- how variables are injected/scoped,
- which theme tokens are expected.
