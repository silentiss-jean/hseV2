# Shadow token bridge — `hse_tokens.shadow.css`

Target file:

- `custom_components/home_suivi_elec/web_static/shared/styles/hse_tokens.shadow.css`

AI-first: which tokens are forwarded into shadow DOM.
Human layer: maintenance.

---

## Purpose

Bridges HA theme tokens into the HSE panel shadow DOM, making variables available to `tokens.css`.

Document:

- which variables are forwarded,
- naming conventions,
- fallback behavior.
